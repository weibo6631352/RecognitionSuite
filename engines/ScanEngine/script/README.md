# 脚本

按平台分目录。

| 目录 | 入口 |
|---|---|
| `windows/开始.bat` | Windows：下载离线包、离线安装、构建打包 |
| `macos/` | 尚未提供 |

双击 `windows\start.cmd`（或 `开始.bat`，它只是调用 start.cmd）。无参数进菜单，结束会 pause。换机先在有网机器选 1 下完 `windows\offline`，拷到离线机再选 2。
